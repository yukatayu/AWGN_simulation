#pragma once
#include <bits/stdc++.h>

using IntPacket = std::vector<long long>;
using Real = long double;
using RealPacket = std::vector<Real>;
using Complex = std::complex<Real>;
using ComplexPacket = std::vector<Complex>;
